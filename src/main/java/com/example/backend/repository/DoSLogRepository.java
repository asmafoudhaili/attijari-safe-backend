package com.example.backend.repository;

import com.example.backend.entity.DoSLog;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DoSLogRepository extends JpaRepository<DoSLog, Long> {
}
